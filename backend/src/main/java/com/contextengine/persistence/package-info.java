/**
 * Persistence-specific adapters and mappings.
 *
 * <p>This package will contain persistence entities, repository implementations, and mappers that
 * translate between storage representations and inward-facing contracts. It may depend on
 * application or domain repository ports and persistence frameworks. Business logic, use-case
 * orchestration, controllers, and transport DTOs must not belong here.</p>
 */
package com.contextengine.persistence;
