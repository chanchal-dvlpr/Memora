/**
 * Reusable, cross-cutting concepts shared by multiple architectural layers.
 *
 * <p>This package contains small exceptions, constants, utilities, and validation support with no
 * business ownership. It should depend only on the JDK or similarly neutral abstractions. Domain
 * rules, Spring components, persistence code, and feature-specific models must not be placed here.</p>
 */
package com.contextengine.shared;
