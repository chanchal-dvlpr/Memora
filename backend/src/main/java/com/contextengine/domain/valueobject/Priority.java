package com.contextengine.domain.valueobject;

/**
 * Categorizes the task implementation order and urgency scale.
 */
public enum Priority {
    /** High urgency, needs immediate attention. */
    HIGH,
    
    /** Medium urgency, normal scheduling priority. */
    MEDIUM,
    
    /** Low urgency, low scheduling priority. */
    LOW
}
