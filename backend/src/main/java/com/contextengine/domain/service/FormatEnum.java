package com.contextengine.domain.service;

/**
 * Standardized output format options for AI context compilation.
 */
public enum FormatEnum {
    /** Structured markdown output. */
    MARKDOWN,
    
    /** Structured JSON payload output. */
    JSON,
    
    /** Structured XML payload output. */
    XML
}
