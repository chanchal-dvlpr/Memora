/**
 * Inbound Model Context Protocol adapter boundary.
 *
 * <p>This package will contain MCP tools, handlers, and protocol translation that invoke application
 * use cases through defined contracts. It may depend on application and neutral shared concepts. MCP
 * protocol details must not leak into the domain, and this package must not contain business rules,
 * direct persistence access, or infrastructure implementations.</p>
 */
package com.contextengine.mcp;
