/**
 * Inbound API adapter boundary.
 *
 * <p>This package will contain controllers, requests, responses, and exception advice that translate
 * external API interactions into application commands and queries. It may depend only on application
 * contracts and neutral shared concerns. It must not call domain repositories directly or contain
 * business rules, persistence logic, or infrastructure implementations.</p>
 */
package com.contextengine.api;
