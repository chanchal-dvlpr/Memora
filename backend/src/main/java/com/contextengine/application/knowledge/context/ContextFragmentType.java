package com.contextengine.application.knowledge.context;

/**
 * Initial types of context fragments extracted from the Knowledge Graph.
 */
public enum ContextFragmentType {
    PROJECT,
    WORKSPACE,
    MODULE,
    DIRECTORY,
    FILE,
    SYMBOL,
    DEPENDENCY
}
