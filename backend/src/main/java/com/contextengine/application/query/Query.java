package com.contextengine.application.query;

/**
 * Marker interface for Query objects requesting system state/resource retrieval.
 * Queries are strictly read-only and must not cause side effects.
 */
public interface Query {
}
