package com.contextengine.application.command;

/**
 * Marker interface for Command objects requesting system state modifications.
 * Commands should remain immutable and carry all parameters required for processing.
 */
public interface Command {
}
