/**
 * Event delivery and dispatching adapters.
 *
 * <p>This package will host publishers, subscribers, and dispatchers that carry domain or
 * application events across process boundaries or internal delivery mechanisms. It may depend on
 * inward event contracts and application ports. Domain event definitions, business reactions, and
 * infrastructure-specific client implementations must remain in their respective layers.</p>
 */
package com.contextengine.events;
