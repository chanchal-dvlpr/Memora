/**
 * Outbound technical adapters and integrations.
 *
 * <p>This package will implement domain or application ports for filesystem access, parsing, search,
 * graph, AI, event, and configuration technologies. It may depend inward on application, domain, and
 * shared contracts, as well as external libraries. Business rules, HTTP request handling, and
 * persistence-domain decisions must not be implemented here.</p>
 */
package com.contextengine.infrastructure;
