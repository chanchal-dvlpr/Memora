/**
 * Scanner feature boundary for filesystem, parser, symbol, and AST workflows.
 *
 * <p>This package will organize scanner-oriented application coordination and feature contracts. It
 * may depend on application and domain abstractions; concrete technology adapters belong in the
 * infrastructure layer. It must not contain HTTP handling, persistence implementations, or business
 * rules that properly belong to the domain.</p>
 */
package com.contextengine.scanner;
